# # config file version
apiVersion: 1

datasources:
  - name: InfluxDB_v1
    type: influxdb
    access: proxy
    database: site
    user: root
    password: root
    url: http://localhost:8086
    jsonData:
      httpMode: GET
